import type { Context } from 'koa';
declare const _default: {
    find(ctx: Context): Promise<any>;
    findOne(ctx: Context): Promise<void>;
    destroy(ctx: Context): Promise<void>;
    getAIMetadataCount(ctx: Context): Promise<any>;
    generateAIMetadata(ctx: Context): Promise<any>;
    /**
     * Generate AI metadata for an explicit selection of files, synchronously.
     *
     * Unlike `generateAIMetadata`, no job is created: the request resolves once
     * every file has been processed and reports the outcome per file, so a single
     * bad id or a non-image in the selection never fails the whole batch.
     *
     * @experimental
     */
    unstable_generateAIMetadata(ctx: Context): Promise<any>;
    getLatestAIMetadataJob(ctx: Context): Promise<any>;
};
export default _default;
//# sourceMappingURL=admin-file.d.ts.map